

function graph() {
  
  function graph(g) {
  }

  graph.axes = function() {
  }

  graph.width = function(width) {
  }

  graph.height = function(height) {
  }

  graph.accept = function(axis, variable, schema) {
  }

  graph.assign = function(axis, variable) {
  }

  graph.schema = function(schema) {
  }

  graph.data = function(data, schema) {
  }

  return graph;
}

